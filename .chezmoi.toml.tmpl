sourceDir = "{{ .chezmoi.sourceDir }}"

[data]
BozePC = "BozePatience"
BozeCompany = "BozeYX"
email = "boze462@gmail.com"

[merge]
    command = "nvim"
    args = [
        "-d",
        {{ printf "%q" "{{ .Destination }}" }},
        {{ printf "%q" "{{ .Source }}" }},
        {{ printf "%q" "{{ .Target }}" }},
    ]

[diff]
    pager = "delta"

